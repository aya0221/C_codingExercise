//p670

/*
friend function:
    defined and used just like anordinary function;
        the dot operator is used when we call a friend function
    no type quantifier is used when i define a friend function

member function:
    called using a object name and the dot operator 
    includes a type quantifier consisting of the class name and the scope resolution operator ::

*/